services:
  celery-beat:
    environment:
      CELERY_BEAT_OWNERSHIP_KEY: ${CELERY_BEAT_OWNERSHIP_KEY:?required}
      CELERY_BEAT_OWNERSHIP_TTL_SECONDS: ${CELERY_BEAT_OWNERSHIP_TTL_SECONDS:-15}
      CELERY_BEAT_OWNERSHIP_RETRY_SECONDS: ${CELERY_BEAT_OWNERSHIP_RETRY_SECONDS:-1}
