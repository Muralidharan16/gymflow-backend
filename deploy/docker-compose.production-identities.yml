services:
  api:
    environment:
      DOERS_PROCESS_PROFILE: api
      CELERY_WORKER_PROFILE: ""
      WORKER_DATABASE_URL: ""
      MAINTENANCE_DATABASE_URL: ""
      NOTIFICATION_EMAIL_PROVIDER_MODE: disabled
      P4C_RESEND_API_KEY: ""
      RESEND_WEBHOOK_SECRET: ${RESEND_WEBHOOK_SECRET:-}
      NOTIFICATION_METRICS_OTLP_ENDPOINT: ""
      SEARCH_PROVIDER_MODE: disabled
      OPENSEARCH_URL: ""
      OPENSEARCH_USERNAME: ""
      OPENSEARCH_PASSWORD: ""
      SEARCH_METRICS_OTLP_ENDPOINT: ""

  celery-worker:
    environment:
      DOERS_PROCESS_PROFILE: worker
      CELERY_WORKER_PROFILE: worker
      DATABASE_URL: ""
      AUTH_DATABASE_URL: ""
      MAINTENANCE_DATABASE_URL: ""
      NOTIFICATION_EMAIL_PROVIDER_MODE: ${NOTIFICATION_EMAIL_PROVIDER_MODE:-disabled}
      P4C_RESEND_API_KEY: ${P4C_RESEND_API_KEY:-}
      NOTIFICATION_EMAIL_FROM: ${NOTIFICATION_EMAIL_FROM:-members@doers.io}
      RESEND_API_BASE_URL: ${RESEND_API_BASE_URL:-https://api.resend.com}
      NOTIFICATION_PROVIDER_TIMEOUT_SECONDS: ${NOTIFICATION_PROVIDER_TIMEOUT_SECONDS:-8}
      RESEND_WEBHOOK_SECRET: ""
      NOTIFICATION_METRICS_OTLP_ENDPOINT: ${NOTIFICATION_METRICS_OTLP_ENDPOINT:-}
      NOTIFICATION_METRICS_EXPORT_INTERVAL_SECONDS: ${NOTIFICATION_METRICS_EXPORT_INTERVAL_SECONDS:-30}
      NOTIFICATION_METRICS_EXPORT_TIMEOUT_SECONDS: ${NOTIFICATION_METRICS_EXPORT_TIMEOUT_SECONDS:-5}
      SEARCH_PROVIDER_MODE: ${SEARCH_PROVIDER_MODE:-disabled}
      OPENSEARCH_URL: ${OPENSEARCH_URL:-}
      OPENSEARCH_INDEX: ${OPENSEARCH_INDEX:-branches-v1}
      OPENSEARCH_USERNAME: ${OPENSEARCH_USERNAME:-}
      OPENSEARCH_PASSWORD: ${OPENSEARCH_PASSWORD:-}
      OPENSEARCH_TIMEOUT_SECONDS: ${OPENSEARCH_TIMEOUT_SECONDS:-5}
      OPENSEARCH_VERIFY_TLS: ${OPENSEARCH_VERIFY_TLS:-true}
      SEARCH_METRICS_OTLP_ENDPOINT: ${SEARCH_METRICS_OTLP_ENDPOINT:-}
      SEARCH_METRICS_EXPORT_INTERVAL_SECONDS: ${SEARCH_METRICS_EXPORT_INTERVAL_SECONDS:-30}
      SEARCH_METRICS_EXPORT_TIMEOUT_SECONDS: ${SEARCH_METRICS_EXPORT_TIMEOUT_SECONDS:-5}

  celery-maintenance-worker:
    environment:
      DOERS_PROCESS_PROFILE: maintenance
      CELERY_WORKER_PROFILE: maintenance
      DATABASE_URL: ""
      AUTH_DATABASE_URL: ""
      WORKER_DATABASE_URL: ""
      NOTIFICATION_EMAIL_PROVIDER_MODE: disabled
      P4C_RESEND_API_KEY: ""
      RESEND_WEBHOOK_SECRET: ""
      NOTIFICATION_METRICS_OTLP_ENDPOINT: ${NOTIFICATION_METRICS_OTLP_ENDPOINT:-}
      NOTIFICATION_METRICS_EXPORT_INTERVAL_SECONDS: ${NOTIFICATION_METRICS_EXPORT_INTERVAL_SECONDS:-30}
      NOTIFICATION_METRICS_EXPORT_TIMEOUT_SECONDS: ${NOTIFICATION_METRICS_EXPORT_TIMEOUT_SECONDS:-5}
      SEARCH_PROVIDER_MODE: disabled
      OPENSEARCH_URL: ""
      OPENSEARCH_USERNAME: ""
      OPENSEARCH_PASSWORD: ""
      SEARCH_METRICS_OTLP_ENDPOINT: ""

  celery-beat:
    environment:
      DOERS_PROCESS_PROFILE: beat
      CELERY_WORKER_PROFILE: ""
      DATABASE_URL: ""
      AUTH_DATABASE_URL: ""
      WORKER_DATABASE_URL: ""
      MAINTENANCE_DATABASE_URL: ""
      NOTIFICATION_EMAIL_PROVIDER_MODE: disabled
      P4C_RESEND_API_KEY: ""
      RESEND_WEBHOOK_SECRET: ""
      NOTIFICATION_METRICS_OTLP_ENDPOINT: ""
      SEARCH_PROVIDER_MODE: disabled
      OPENSEARCH_URL: ""
      OPENSEARCH_USERNAME: ""
      OPENSEARCH_PASSWORD: ""
      SEARCH_METRICS_OTLP_ENDPOINT: ""

  flower:
    environment:
      DOERS_PROCESS_PROFILE: beat
      CELERY_WORKER_PROFILE: ""
      DATABASE_URL: ""
      AUTH_DATABASE_URL: ""
      WORKER_DATABASE_URL: ""
      MAINTENANCE_DATABASE_URL: ""
      NOTIFICATION_EMAIL_PROVIDER_MODE: disabled
      P4C_RESEND_API_KEY: ""
      RESEND_WEBHOOK_SECRET: ""
      NOTIFICATION_METRICS_OTLP_ENDPOINT: ""
      SEARCH_PROVIDER_MODE: disabled
      OPENSEARCH_URL: ""
      OPENSEARCH_USERNAME: ""
      OPENSEARCH_PASSWORD: ""
      SEARCH_METRICS_OTLP_ENDPOINT: ""