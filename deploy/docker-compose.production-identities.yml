services:
  api:
    environment:
      DOERS_PROCESS_PROFILE: api
      CELERY_WORKER_PROFILE: ""
      WORKER_DATABASE_URL: ""
      MAINTENANCE_DATABASE_URL: ""
      SEARCH_PROVIDER_MODE: disabled
      OPENSEARCH_URL: ""
      OPENSEARCH_USERNAME: ""
      OPENSEARCH_PASSWORD: ""
      SEARCH_METRICS_OTLP_ENDPOINT: ""

  celery-worker:
    environment:
      DOERS_PROCESS_PROFILE: worker
      CELERY_WORKER_PROFILE: worker
      DATABASE_URL: ""
      AUTH_DATABASE_URL: ""
      MAINTENANCE_DATABASE_URL: ""
      SEARCH_PROVIDER_MODE: ${SEARCH_PROVIDER_MODE:-disabled}
      OPENSEARCH_URL: ${OPENSEARCH_URL:-}
      OPENSEARCH_INDEX: ${OPENSEARCH_INDEX:-branches-v1}
      OPENSEARCH_USERNAME: ${OPENSEARCH_USERNAME:-}
      OPENSEARCH_PASSWORD: ${OPENSEARCH_PASSWORD:-}
      OPENSEARCH_TIMEOUT_SECONDS: ${OPENSEARCH_TIMEOUT_SECONDS:-5}
      OPENSEARCH_VERIFY_TLS: ${OPENSEARCH_VERIFY_TLS:-true}
      SEARCH_METRICS_OTLP_ENDPOINT: ${SEARCH_METRICS_OTLP_ENDPOINT:-}
      SEARCH_METRICS_EXPORT_INTERVAL_SECONDS: ${SEARCH_METRICS_EXPORT_INTERVAL_SECONDS:-30}
      SEARCH_METRICS_EXPORT_TIMEOUT_SECONDS: ${SEARCH_METRICS_EXPORT_TIMEOUT_SECONDS:-5}

  celery-maintenance-worker:
    environment:
      DOERS_PROCESS_PROFILE: maintenance
      CELERY_WORKER_PROFILE: maintenance
      DATABASE_URL: ""
      AUTH_DATABASE_URL: ""
      WORKER_DATABASE_URL: ""
      SEARCH_PROVIDER_MODE: disabled
      OPENSEARCH_URL: ""
      OPENSEARCH_USERNAME: ""
      OPENSEARCH_PASSWORD: ""
      SEARCH_METRICS_OTLP_ENDPOINT: ""

  celery-beat:
    environment:
      DOERS_PROCESS_PROFILE: beat
      CELERY_WORKER_PROFILE: ""
      DATABASE_URL: ""
      AUTH_DATABASE_URL: ""
      WORKER_DATABASE_URL: ""
      MAINTENANCE_DATABASE_URL: ""
      SEARCH_PROVIDER_MODE: disabled
      OPENSEARCH_URL: ""
      OPENSEARCH_USERNAME: ""
      OPENSEARCH_PASSWORD: ""
      SEARCH_METRICS_OTLP_ENDPOINT: ""

  flower:
    environment:
      DOERS_PROCESS_PROFILE: beat
      CELERY_WORKER_PROFILE: ""
      DATABASE_URL: ""
      AUTH_DATABASE_URL: ""
      WORKER_DATABASE_URL: ""
      MAINTENANCE_DATABASE_URL: ""
      SEARCH_PROVIDER_MODE: disabled
      OPENSEARCH_URL: ""
      OPENSEARCH_USERNAME: ""
      OPENSEARCH_PASSWORD: ""
      SEARCH_METRICS_OTLP_ENDPOINT: ""
